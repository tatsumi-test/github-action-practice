name: Hello
on: push
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello World"
      - run: echo "${ github.ref }"
      - run: ls -al
      - uses: actions/checkout@v4
        with:
          ref: master
      - run: ls -al
